# -*- coding: utf-8 -*-

"""
Built-in functions, example

abs, all, any, ascii, bin, bool, bytearray, callable, bytes, chr,
compile, classmethod, complex, delattr, dict, dir, divmod, enumerate,
staticmethod, filter, eval, float, format, frozenset, getattr, globals,
exec, hasattr, help, hex, hash, input, id, isinstance, int, issubclass,
iter, list, locals, len, max, min, map, next, memoryview, object, oct,
ord, open, pow, print, property, range, repr, reversed, round, set, setattr,
slice, sorted, str, sum, tuple, vars, zip, __import__, super

3.10
aiter
"""